spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/auth_db
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:replace-me}
            client-secret: ${GOOGLE_CLIENT_SECRET:replace-me}
            scope:
              - openid
              - email
              - profile
            redirect-uri: http://localhost:8080/auth/login/oauth2/code/{registrationId}

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

jwt:
  secret: qC_siddhesh_9fA7xL2pQw8ZrT6mN4vYkH1uB3eD5Jc
  expiration: 86400000
  access-expiration: 900000
  refresh-expiration: 604800000

oauth2:
  authorized-redirect-uri: http://localhost:5173/oauth2/redirect

quickcart:
  admin:
    setup-key: ${ADMIN_SETUP_KEY:}
